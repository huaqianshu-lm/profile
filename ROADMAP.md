# 花千树个人站｜ROADMAP

## 当前阶段

- 静态单页站点的核心叙事、六个作品和深浅主题基线已稳定。
- 项目级执行规范已确立为面向 AI／开发者的 `CLAUDE.md`。
- 既有整改“阶段 5 已完成”经复验无效：阶段渲染与 CSS 仅形式拆分，硬验收整改待执行。
- 四项 UI 回归已完成静态修复与验收：阶段残影、About 首屏闪现与浅色样式级联；Works 详情开关、覆盖层与音频详情初始化已修复，等待页面复测。

## 进行中

- 阶段 2 已按封闭修订版完成静态实施：Canvas 跳转归零、独立续帧组合、纯 frame 与作品悬停状态的帧前清理已落地，等待独立只读验收；不得据此标记阶段完成。
- 浏览器回归验证：等待用户明确授权或手动反馈后执行桌面／移动、深色／浅色检查。

## 下一步

- 对封闭修订后的阶段 2 执行独立只读验收，复核阶段集合、Canvas 跳转、续帧归属与纯 frame 契约。
- 获得浏览器检查授权后，执行桌面／移动、深浅主题、详情 FLIP、音视频和性能回归。
- 重构真正稳定后，继续完成可访问性、SEO／OG 与部署准备。

## 阻塞

- 阶段 2 的静态实施等待独立验收；CSS 实体迁移仍未通过硬验收，整改不得标记完成。
- 浏览器回归验证待授权；未进行视觉、媒体与性能结论认定。

## 已完成

- 2026-09-08：修复 Works 详情预览初始化异常：恢复音频播放器的活动音频与动画帧模块状态，详情构建前的媒体清理不再抛出 `ReferenceError`；模块加载与清理函数静态回归通过。
- 2026-09-08：补充修复 Works 详情打开链路：详情控制器改用渲染帧已确认的时间线状态，桥接文案层不再拦截底部卡片的指针事件；静态边界、语法与差异检查通过。
- 2026-09-08：完成四项 UI 回归的静态修复与验收：非活动阶段状态显式收敛，Works 卡片与详情共用开关判定，About 首帧默认隐藏，后加载样式边界补齐浅色主题覆盖；浏览器视觉回归尚未执行。
- 2026-09-08：确立重构方案归档约定：今后所有重构方案统一保存在忽略的 `drafts/`，不得放入项目根目录。
- 2026-09-08：项目级 `CLAUDE.md` 已与现有目录和模块职责同步：重构方案位于忽略的 `drafts/`，核心文档须纳入版本控制，修改范围按 HTML／脚本／样式职责划分。
- 2026-09-08：完成静态验收并形成整改方案；确认初次实施保留了单页入口、资源与数据契约，但未达到阶段渲染、RAF、CSS 与详情预览的职责拆分标准。
- 2026-09-08：完成整改阶段 1，六类详情预览迁入独立模块，预览分派、稳定 DOM 注册与主题首屏例外边界通过静态检查。
- 2026-09-08：以硬验收整改方案替代原整改方案，新增阶段职责矩阵、CSS 移动契约、反向断言与独立验收门槛。
- 2026-09-08：修订整改方案的阶段 2：消除非相邻阶段与 Canvas 淡出之间的契约冲突，新增冻结边界样例、Canvas 持续层归属和裸时间常量反向断言。
- 2026-09-08：再次封闭整改方案的阶段 2：固定 Canvas 截止时的无条件样式同步、Canvas 与语义阶段的续帧分流，以及 `scheduler → state → renderer → frame` 的只读帧数据流，并新增跳转回归断言。
- 2026-09-07：建立并重组项目级 `CLAUDE.md`，明确单页架构、作品内容保护、交互约束与修改流程。

## 最近验证

- 2026-09-08：音频详情初始化静态回归通过：`cleanupDetailAudio()` 可在未打开音乐详情时安全执行，播放器活动音频与动画帧状态均有唯一模块级声明；相关三个模块语法、ES Module 加载和 `git diff --check` 通过。未执行浏览器自动化或页面复测。
- 2026-09-08：Works 详情二次修复静态检查通过：`.31／.40／.50` 均为可开启边界，详情开关仅读取渲染帧时间线状态，`bridge` 已显式禁用指针命中；未执行浏览器自动化或页面复测。
- 2026-09-08：四项 UI 回归最终静态验收通过：导航目标 `.04／.40／.82` 的阶段集合与 Works 开关边界符合预期；全量 JavaScript 语法、本地 ES Module import、HTML 本地资源路径和 `git diff --check` 均通过。未执行浏览器自动化或视觉回归。
- 2026-09-08：`CLAUDE.md` 已明确未来重构方案的唯一归档位置为 `drafts/`，并禁止置于项目根目录。
- 2026-09-08：目录与文档规范复核通过：核心文档和两份 `drafts/` 方案均存在，`CLAUDE.md` 变更通过 `git diff --check`；暂存的 `PROJECT_HANDOFF.md` 有两处既有行尾空格，本次未改写其内容。
- 2026-09-08：修复阶段 2 的 Works 模块直接状态写入；悬停清理已移至帧前状态 action，新增 stages 状态赋值反向断言、悬停保留／清理回归检查与全量 JavaScript 语法检查均通过；待独立只读验收。
- 2026-09-08：复验确认 15 个 ES Modules、7 个 CSS 文件、57 个唯一 DOM id、单 module 入口和资源路径存在；同时确认阶段渲染、主 RAF、CSS 实体拆分与详情预览拆分未达到方案标准。
- 2026-09-08：整改阶段 1 静态检查通过，7 个预览模块语法与本地 import 可解析，`previews/index.js` 仅保留 type 分派与媒体清理。
- 2026-09-08：复验失败：`frame.js` 仍承载跨阶段写入，五个阶段文件仅作活动标记；`base.css` 保留被复制到五个边界文件的完整规则。已以硬验收整改方案取代原通过结论。
- 2026-09-08：独立静态验收发现阶段 2 的两项缺口：Method 时段并行调用非相邻 Seed／Grow，且阶段文件残留未命名的时间／stagger 常量；方案已据此修订，尚未重新实施。
